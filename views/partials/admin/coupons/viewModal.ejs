<!-- View Coupon Details Modal -->
<div id="viewModal" class="modal hidden fixed z-[1000] left-0 top-0 w-full h-full overflow-auto bg-black bg-opacity-50 backdrop-blur-sm">
  <div class="modal-content bg-white rounded-2xl w-[90%] max-w-2xl max-h-[90vh] overflow-y-auto shadow-2xl my-12 mx-auto animate-slideDown">
    <div class="flex justify-between items-center px-6 py-6 border-b border-gray-200">
      <h2 class="m-0 text-2xl text-gray-900 font-semibold">Coupon Details</h2>
      <button class="modal-close bg-transparent border-none text-2xl cursor-pointer text-gray-500 w-8 h-8 flex items-center justify-center rounded-md transition-all hover:bg-red-50 hover:text-red-600" 
              onclick="closeViewModal()">
        <i class="bi bi-x-lg"></i>
      </button>
    </div>
    
    <div id="viewModalContent" class="px-6 py-6">
      <!-- Content will be loaded dynamically -->
    </div>
  </div>
</div>

<style>
  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-slideDown {
    animation: slideDown 0.3s ease-out;
  }
  
  .modal.show {
    display: flex !important;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }
</style>